import formBinDefaults from '@features/Packing/consts/formBinDefaults'
import formItemDefaults from '@features/Packing/consts/formItemDefaults'

export default {
  bins: [
    formBinDefaults,
  ],
  items: [
    formItemDefaults,
  ],
}